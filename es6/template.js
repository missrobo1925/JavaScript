let s1 ="swati";
let s2='priya';
let s3=`jaiswal`;
console.log(s1,typeof s1);
console.log(s2,typeof s2);
// template literal allows to even use the variables within a string 

console.log(`Hello ${s1}!`);
//so we can do it by using backticks instead of actually concatenating the string every time, directly by using the varibales.

